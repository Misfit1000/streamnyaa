import DesktopBookmarkButton from './DesktopBookmarkButton';
export default function DesktopCatalogActions({ anime }: { anime: any }) { return <DesktopBookmarkButton anime={anime} label className="mt-2 min-h-10 w-full" />; }
